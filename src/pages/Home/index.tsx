import AboutSection from '../../components/AboutSection';
import './styles.css';

function Home() {
    return (
        <main className="home-page page-padding">
            <AboutSection />
        </main>
    );
}

export default Home;
